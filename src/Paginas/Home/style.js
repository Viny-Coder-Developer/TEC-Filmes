import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#340056',
    alignItems: "center"
  },
  autor: {
    color: 'white',
    fontWeight: 'bold',
    textAlign: 'center',
    marginLeft: -180,
    marginTop: 50,
    fontSize: 15
  }
})

export default styles;